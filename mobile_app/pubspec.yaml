name: mobile_app
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^22.3.1
  alarm: ^4.1.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  timezone: ^0.11.1
  flutter_timezone: ^3.0.1 # Add this line  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/alarm.mp3